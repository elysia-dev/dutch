export type CoinPriceResponse = {
  elysia: {
    usd: number;
  };
  ethereum: {
    usd: number;
  };
  binancecoin: {
    usd: number;
  };
  dai: {
    usd: number;
  };
};

export type ELPriceResponse = {
  elysia: {
    usd: number;
  };
};
